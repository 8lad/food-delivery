import { css } from '@emotion/react';
import { MainColors } from '../../../utils/styleConstants';

export const authErrorMessageStyles = css`
  color: ${MainColors.MAIN_ERROR_COLOR};
  padding: 10px;
  font-size: 16px;
  line-height: 24px;
`;
